CREATE TABLE
    db_budgetfit.types (
        typeID int NOT NULL,
        PRIMARY KEY (typeID)
    ) ENGINE = INNODB,
    CHARACTER SET utf8mb4,
    COLLATE utf8mb4_0900_ai_ci,
    COMMENT = 'Intended to hold diet metadata';